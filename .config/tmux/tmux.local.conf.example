# Local Configuration Overrides
# This file is sourced by tmux.conf if it exists at ~/.config/tmux/tmux.local.conf
# Use this for machine-specific settings, secrets, or experimental changes.

# Example: Change prefix key on this machine
# unbind C-Space
# set -g prefix C-a
# bind C-a send-prefix

# Example: Override theme colors
# set -g @catppuccin_flavour 'latte'

# Example: Add machine-specific status
# set -g status-right "Host: #H"
