#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = $gaps_in
    gaps_out = $gaps_out

    border_size = $border_size

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = $active_border_color
    col.inactive_border = $inactive_border_color

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = $rounding

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1.0

    shadow {
        enabled = true
        range = $shadow_range
        render_power = 3
        color = $shadow_color
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = $blur_size
        passes = 1

        vibrancy = $blur_vibrancy
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = yes, please :)

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = easeOutQuint,$bezier_easeOutQuint
    bezier = easeInOutCubic,$bezier_easeInOutCubic
    bezier = linear,$bezier_linear
    bezier = almostLinear,$bezier_almostLinear
    bezier = quick,$bezier_quick

    animation = global, $animation_global
    animation = border, $animation_border
    animation = windows, $animation_windows
    animation = windowsIn, $animation_windowsIn
    animation = windowsOut, $animation_windowsOut
    animation = fadeIn, $animation_fadeIn
    animation = fadeOut, $animation_fadeOut
    animation = fade, $animation_fade
    animation = layers, $animation_layers
    animation = layersIn, $animation_layersIn
    animation = layersOut, $animation_layersOut
    animation = fadeLayersIn, $animation_fadeLayersIn
    animation = fadeLayersOut, $animation_fadeLayersOut
    animation = workspaces, $animation_workspaces
    animation = workspacesIn, $animation_workspacesIn
    animation = workspacesOut, $animation_workspacesOut
}

# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrulev2 = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrulev2 = rounding 0, floating:0, onworkspace:w[tv1]
# windowrulev2 = bordersize 0, floating:0, onworkspace:f[1]
# windowrulev2 = rounding 0, floating:0, onworkspace:f[1]

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = 0 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
}