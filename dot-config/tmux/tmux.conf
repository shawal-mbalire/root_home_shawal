# -------------------------
# General Options
# -------------------------

if-shell '[ "$(uname -s)" = "Darwin" ]' \
  'set-option -g default-shell /opt/homebrew/bin/nu' \
  'set-option -g default-shell /usr/bin/nu'
set-option -g default-terminal 'tmux-256color'
set-option -g terminal-overrides ',xterm-256color:RGB'

set -g mouse on
set -g mode-keys vi
set -g base-index 1              # start indexing windows at 1 instead of 0
set -g escape-time 0             # zero-out escape time delay
set -g set-clipboard on          # use system clipboard
set -g pane-base-index 1
set -g status-bg default # Force transparent status bar background
set -g renumber-windows on       # renumber all windows when any window is closed
set -g history-limit 1000000     # increase history size (from 2,000)
set -g update-environment "SSH_AUTH_SOCK SSH_AGENT_PID SSH_CONNECTION DISPLAY KRB5CCNAME WINDOWID"
set -g detach-on-destroy off     # don't exit from tmux when closing a session
set -g status-style bg=default # Force transparent status bar background
set -g pane-border-style 'fg=brightblack,bg=default'
set -g pane-active-border-style 'fg=magenta,bg=default'


# -------------------------
# keybinds.conf 
# -------------------------
# Key Bindings
set -g prefix C-Space
# set r to reload tmux
unbind r 
bind C-r source-file ~/.config/tmux/tmux.conf
bind C-n command-prompt -I "#W" "rename-window '%%'"

# navigation
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

# Resize panes using Alt + h, j, k, l (No prefix needed)
bind -n M-h resize-pane -L 5
bind -n M-j resize-pane -D 5
bind -n M-k resize-pane -U 5
bind -n M-l resize-pane -R 5

# Copy Mode --> prefix + [
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle
bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel

# Window Splitting
bind '"' split-window -v -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"


# -------------------------
# skin.conf 
# -------------------------
# Catppuccin Theme Settings

set -g status-position top
set -g @catppuccin_flavour 'mocha'
set -g @catppuccin_status_fill "icon"
set -g @catppuccin_date_time_icon "󰃰"
set -g @catppuccin_date_time_text "%H:%M"
set -g @catppuccin_window_default_text "#W"
set -g @catppuccin_window_left_separator ""
set -g @catppuccin_window_right_separator ""
set -g @catppuccin_status_left_separator  ""
set -g @catppuccin_status_right_separator ""
set -g @catppuccin_window_middle_separator "█"
set -g @catppuccin_date_time_color "$thm_blue"
set -g @catppuccin_status_background "default"
set -g @catppuccin_window_current_fill "number"
set -g @catppuccin_window_default_fill "number"
set -g @catppuccin_status_connect_separator "no"
set -g @catppuccin_window_number_position "left"
set -g @catppuccin_status_right_separator_inverse "no"
set -g @catppuccin_directory_text "#{b:pane_current_path}"
set -g @catppuccin_window_current_text "#{pane_current_path}"

set -g @catppuccin_status_modules_left    "session"
set -g @catppuccin_status_modules_right   "date_time battery"


# -------------------------
# plugins
# -------------------------

set-environment -g TMUX_PLUGIN_MANAGER_PATH "$HOME/.config/tmux/plugins"

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-battery'
set -g @plugin 'sainnhe/tmux-fzf'
set -g @plugin 'wfxr/tmux-fzf-url'
set -g @plugin 'omerxx/tmux-floax'
set -g @plugin 'omerxx/tmux-sessionx'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'shawal-mbalire/catppuccin-tmux'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @resurrect-strategy-nvim 'session'

# FZF URL
set -g @fzf-url-history-limit '2000'
set -g @fzf-url-fzf-options '-p 60%,30% --prompt="   " --border-label=" Open URL "'

# Floax
set -g @floax-bind 'p'
set -g @floax-width '80%'
set -g @floax-height '80%'
set -g @floax-text-color 'blue'
set -g @floax-change-path 'true'
set -g @floax-border-color 'magenta'

# SessionX
unbind s 
set -g @sessionx-bind 's'
set -g @sessionx-zoxide-mode 'on'
set -g @sessionx-auto-accept 'off'
set -g @sessionx-window-width '80%'
set -g @sessionx-window-height '80%'
set -g @sessionx-filter-current 'false'
set -g @sessionx-bind-zo-new-window 'ctrl-y'
set -g @sessionx-custom-paths-subdirectories 'false'
set -g @sessionx-fzf-builtin-tmux 'on'

# Resurrect / Continuum
set -g @continuum-restore 'on'
set -g @continuum-boot 'on'
set -g @continuum-save-interval '5'
# This works best if you have 'tpope/vim-obsession' installed in nvim
set -g @resurrect-strategy-nvim 'session'
set -g @resurrect-capture-pane-contents 'on'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run-shell 'if [ ! -x "$TMUX_PLUGIN_MANAGER_PATH/tpm/tpm" ]; then mkdir -p "$TMUX_PLUGIN_MANAGER_PATH" && git clone https://github.com/tmux-plugins/tpm "$TMUX_PLUGIN_MANAGER_PATH/tpm"; fi'
run-shell '~/.config/tmux/plugins/tpm/bin/install_plugins'
run '~/.config/tmux/plugins/tpm/tpm'

# Run battery interpolation after all plugins are loaded
if-shell '[ -x ~/.config/tmux/plugins/tmux-battery/battery.tmux ]' 'run-shell ~/.config/tmux/plugins/tmux-battery/battery.tmux'
